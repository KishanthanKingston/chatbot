[project]
name = "chatbot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "lxml>=6.0.3",
    "mistralai>=1.5.2",
    "python-dotenv>=1.0.1",
]

[dependency-groups]
dev = [
    "pre-commit>=3.5.0",
]
